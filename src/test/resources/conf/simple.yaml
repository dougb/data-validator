---

numKeyCols: 2
numErrorsToReport: 1
detailedErrors: false

vars:
  - name: ONE
    value: "1"

tables:
  - orcFile: ${DATA_DIR}/testData.orc
    checks:
      - type: rowCount
        minNumRows: 90

      - type: negativeCheck
        column: ${COL}
